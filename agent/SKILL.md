---
name: flightwatch
description: Слежение за рейсами - смотреть статус рейсов, загрузку аэропорта целиком, здоровье источников, историю в базе, живые табло; добавлять и снимать рейсы. Использовать при любом вопросе про рейс, вылет, прилёт, задержку, про обстановку в аэропорту, табло, СМС по рейсу или про сам демон flightwatch.
user-invocable: true
---

# Слежение за рейсами

Демон `flightwatch` следит за рейсами по FR24 и табло аэропортов и шлёт
человеку Telegram и СМС. Ты не имеешь доступа к его каталогу: это
намеренно, границу ставит ядро. Всё общение идёт через две команды.

## Чтение - без подтверждения

Запускай свободно, эти команды ничего не меняют.

    fwctl status                  # рейсы и их состояние
    fwctl log --lines 80          # хвост журнала демона
    fwctl sources --hours 24      # живы ли источники: опросов / удачных
    fwctl config                  # настройки, секреты скрыты
    fwctl sql "SELECT ..."        # только SELECT, автоматический LIMIT 200
    fwctl board --airport TIV --source montenegro --leg DEP
    fwctl history --flight XX1234   # пунктуальность: считает служба

У `fwctl config` ответ состоит из двух частей: `effective` - значения, с
которыми демон РАБОТАЕТ СЕЙЧАС (умолчания кода плюс правки из файла), и
`overridden_in_file` - список ключей, которые реально переопределены.
Отвечая про настройку, бери число из `effective`; отсутствие ключа в
`overridden_in_file` значит «значение по умолчанию», а не «не задано».

Вывод у всех команд - JSON. Разбирай его **`jq`**, а не питоном: `jq`,
`grep`, `head`, `cut` и прочие читающие утилиты разрешены и проходят молча,
а `python3`, `awk` и `sed` каждый раз поднимают запрос подтверждения у
человека. В конвейере разрешён должен быть КАЖДЫЙ сегмент, так что одна
питоновская вставка портит всю команду.

    fwctl status | jq -r '.result.flights[] | select(.tracked) | .title'
    fwctl sources --hours 24 | jq -r '.result.rows[] | "\(.source) \(.ok)/\(.polls)"'

`fwctl board` дёргает табло тем же кодом, что и демон. Источники:
`montenegro` (TIV), `zvartnots` (EVN, LWN), `beg` (Белград),
`tav` (ALA, TBS, ESB, ADB и другие аэропорты TAV). Российских аэропортов
(SVO, DME, VKO, KRR, STW) среди них НЕТ - они идут через FR24, и `board`
по ним не работает. Как отвечать про них - ниже.

## «Как дела в аэропорту» - это НЕ про рейсы человека

Когда спрашивают «как дела в SVO», «что в аэропорту», «там всё спокойно»,
речь идёт о загрузке аэропорта ЦЕЛИКОМ, а не о двух отслеживаемых рейсах.
Ответить статусом своих рейсов - значит не ответить на вопрос.

Демон считает это сам по всей доске и пишет в таблицу `airport_status`:

    fwctl sql "SELECT airport, checked_at, flights, avg_delay, max_delay,
               cancelled, share_bad, state FROM airport_status
               WHERE airport='SVO' ORDER BY checked_at DESC LIMIT 1"

Колонки: `flights` - сколько рейсов попало в окно (-2 ч … +4 ч),
`avg_delay` и `max_delay` - средняя и худшая задержка в минутах,
`cancelled` - отмен в окне, `share_bad` - доля проблемных в процентах,
`state` - оценка: `norm` = норма, `stress` = напряжённо, `outage` = СБОЙ.

Пороги: `stress` от средней 45 мин, `outage` от 90 мин либо от 4 отмен в
окне; обратно в норму - ниже 30. Пороги входа и выхода разные намеренно.

Отвечая, назови ЧИСЛА и время замера, а не только слово. «В SVO норма»
без цифр - это не ответ. Если по аэропорту строк нет вовсе, так и скажи:
демон следит за аэропортом только когда до рейса оттуда меньше 48 часов.

Динамику за сутки бери так:

    fwctl sql "SELECT DATE_FORMAT(checked_at,'%H:%i') t, flights, avg_delay,
               cancelled, state FROM airport_status WHERE airport='SVO'
               AND checked_at > NOW() - INTERVAL 24 HOUR
               ORDER BY checked_at DESC LIMIT 24"

## Управление рейсами - без подтверждения

Добавлять и снимать рейсы можно свободно, кнопки не будет.

    fwctl-write add-flight --flight XX1234 --airport AAA --date 2026-01-01 \
        --direction departure --title "Город А - Город Б" --to BBB
    fwctl-write remove-flight --flight XX1234 --date 2026-01-01

Свобода тут не значит "делай молча". Снятый рейс - это несостоявшиеся
предупреждения о задержке в день вылета, поэтому:

- снимай рейс только когда об этом попросили прямо, а не потому, что он
  показался тебе лишним или завершённым;
- завершённые рейсы удалять не надо, демон разбирается с ними сам;
- добавив или сняв рейс, скажи об этом в ответе одной строкой.

О любом изменении списка человек получает уведомление в Telegram, так что
незаметно это всё равно не пройдёт.

## Настройки демона - через кнопку

Отдельная команда, каждый запуск подтверждается:

    fwctl-config set --key tg_shift_min --value 10

Ключи: `notify`, `tg_shift_min`, `sms_shift_min`, `briefing_within_h`,
`airport_alert_within_h`, `sms_ttl_s`. Причина отдельной команды -
`notify false` выключает уведомления сразу по всем рейсам, а это уже не
управление рейсами.

После правки демон перечитывает конфиг сам, цикл 90 секунд. Не перезапускай
его и не проси перезапустить.

Снятый рейс исчезает из слежения, но его строка остаётся в таблице
`flights`, и вся история по нему сохраняется - это нарочно. Не считай
наличие строки в базе признаком того, что за рейсом следят: смотри
`fwctl status`.

## Как отвечать про рейсы

- `airport` - это аэропорт, чьё табло слушать, а не маршрут. Для вылета
  это аэропорт отправления, для прилёта - назначения.
- `date` - местная дата этого аэропорта, не твоя и не московская.
- Прежде чем сказать «задержки нет», посмотри `fwctl sources`: молчание
  источника и отсутствие задержки - разные вещи, и путать их нельзя.
- Различай три разных вопроса и не подменяй один другим: «что с моим
  рейсом» - это `fwctl status`; «как дела в аэропорту» - это
  `airport_status`; «живы ли источники» - это `fwctl sources`.
- Числа из базы давай с количеством наблюдений. Одна точка - не измерение.
- База - **MariaDB, а не SQLite**: `sqlite_master` там нет. Список таблиц:
  `fwctl sql "SELECT table_name FROM information_schema.tables WHERE
  table_schema='flightwatch'"`. Поля - `SELECT * FROM <таблица> LIMIT 3`.
  Основные: `flight_history`, `punctuality`, `source_polls`,
  `observations`, `airport_status`, `wave_stats`.

## Задержки и риск: считает `fwctl history`, а не ты

На любой вопрос «часто ли опаздывает», «какой риск завтра», «как летал
раньше» - **сначала `fwctl history --flight НОМЕР`**. Не собирай статистику
запросами через `sql` и не считай средние сам: там ошибаются одинаково -
усредняют вместе рейсы, летающие по разному расписанию, и ставят среднее за
месяц рядом со списком за неделю.

    fwctl history --flight XX1234            # ближайший будущий вылет
    fwctl history --flight XX1234 --date 2026-01-01

Что приходит и как этим пользоваться:

- `target` - какой именно вылет оценивается.
- `schedule_groups` - один номер может летать по разному расписанию в
  разные дни; группы посчитаны отдельно, у нужной стоит
  `is_target_group: true`.
- **`target_group` - главное число ответа.** Это те же дни, что и
  оцениваемый рейс. Общее среднее по всем группам для прогноза не годится.
- `overall` и `last7` - за всё окно и за последнюю неделю. Если расходятся,
  называй оба.
- `median` рядом с `avg`: когда среднее заметно выше медианы, его тянут
  выбросы, и полезнее счёт `ge30` / `ge60` - «6 вылетов из 23 с задержкой
  30+ минут». Это применимо, «31.3 в среднем» - нет.
- `recent[].consistent: false` - строка, где разница времён не сходится с
  задержкой. Скажи об этом вслух, не пересказывай её молча.
- `note` - готовые предупреждения по этому конкретному рейсу. Прочитай их
  перед тем, как отвечать, и не противоречь им.

Числа из ответа переписывай как есть. Своих не досчитывай: если чего-то не
хватает, скажи, чего именно, а не выводи из имеющегося.

## Чего делать не нужно

- Не выдумывай статус рейса, если `fwctl status` вернул пусто: скажи, что
  демон этот рейс ещё не видел на табло.
- Не отправляй человеку СМС от своего имени - за уведомления отвечает демон.
